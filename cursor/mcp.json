{
  "mcpServers": {
    "workflow-toolkit": {
      "command": "bash",
      "args": [
        "-lc",
        "exec \"$HOME/.local/share/workflow-toolkit/scripts/run-cursor-mcp.sh\" \"$0\"",
        "${workspaceFolder}"
      ]
    }
  }
}

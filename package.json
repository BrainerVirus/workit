{
  "name": "workflow-toolkit-opencode",
  "version": "0.3.19",
  "private": true,
  "type": "module",
  "main": "./src/plugin.ts",
  "exports": {
    ".": "./src/plugin.ts"
  },
  "scripts": {
    "test": "bun test",
    "test:cursor": "npm --prefix cursor/mcp test",
    "typecheck": "tsc --noEmit",
    "check": "bun test && tsc --noEmit && npm --prefix cursor/mcp ci && npm --prefix cursor/mcp test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.17.7"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "22.13.9",
    "typescript": "5.8.2"
  }
}
